@model IEnumerable<TakipEczane.Models.Doktor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Adısoyadı)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Diplomano)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Brans)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kurum)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Adısoyadı)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Diplomano)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Brans)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kurum)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
